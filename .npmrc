# En CI, pnpm activa --frozen-lockfile por defecto y falla si el lockfile
# no cuadra con package.json. Aqui la tolerancia es intencional: lockfile v9
# generado por pnpm@9, y Vercel actualiza a demanda.
frozen-lockfile=false
